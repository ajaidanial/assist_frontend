/* eslint-disable no-unused-vars */
export const styles = (theme) => ({
  root: {},
  textField: {
    marginTop: theme.spacing(3)
  },
  cardFooter: {
    float: 'right'
  },
  formSelect: {
    marginTop: theme.spacing(3)
  }
})
